# Example environment file.
# Copy to `.env.local` (not committed) and fill values.
#
# Local Supabase (recommended for dev/testing):
# - Run: `supabase start`
# - Then: `supabase status` (copy URL + keys into `.env.local`)
#
# App
NEXT_PUBLIC_SITE_URL=http://localhost:6006
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# E2E / server-only
SUPABASE_SERVICE_ROLE_KEY=

# Optional (KV caching)
KV_REST_API_URL=
KV_REST_API_TOKEN=

